# Initialize a matrix

A=[[1,2,3],[4,5,6],[7,8,9]]

# Initialize a vector

V=[1,2,3]

# get the dimension of matrix and the vector

# m=rows and n = columns 

[m,n]=len(A),len(A[0])

dim_V=len(V)

print(m,n,dim_V)
